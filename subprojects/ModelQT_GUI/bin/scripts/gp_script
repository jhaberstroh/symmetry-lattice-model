#! /bin/sh
cd scripts/
rm plotfile.gp
# WD = pwd
echo set title \"$1\" > plotfile.gp
echo plot \"../$1\" u 0:$3 w lp >> plotfile.gp
echo call \"export.gp\" \"../$2.png\" >> plotfile.gp
# echo pause -1 >> plotfile.gp
gnuplot plotfile.gp

